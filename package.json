{
  "name": "reacts-components-typescript-demo",
  "version": "0.0.9",
  "description": "react common components library",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:entries": "rimraf ./lib/*.*",
    "clean:dist": "rimraf ./dist",
    "generate-entries": "npm run clean:entries && node build-scripts/generate-components-entries",
    "copy": "node build-scripts/copy ./ ./dist \".npmignore\" \".package.json\"",
    "build": "npm run clean:dist && npm run generate-entries && tsc && npm run copy",
    "bump": "npm version patch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^16.0.28",
    "awesome-typescript-loader": "^3.4.1",
    "recursive-copy": "^2.0.7",
    "rimraf": "^2.6.2",
    "ts-loader": "^3.2.0",
    "typescript": "^2.6.2",
    "webpack": "^3.10.0"
  },
  "dependencies": {
    "react": "^16.2.0"
  }
}
